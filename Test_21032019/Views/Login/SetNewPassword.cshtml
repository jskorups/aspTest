@model Test_21032019.Models.passwordResetModel

@{
    ViewBag.Title = "SetNewPassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>SetNewPassword</h2>

@using (Html.BeginForm("SetNewPassword","Login",FormMethod.Post)) 
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>passwordResetModel</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.adresEmail)
    @Html.HiddenFor(model => model.guid)
    <div class="form-group">
        <label>Hasło</label>
        <div class="col-md-10">
            <input type="password" name="newPassword" />
        </div>
    </div>

    <div class="form-group">
        <label>Powtórz hasło</label>
        <div class="col-md-10">
            <input type="password" name="newPasswordConfirm" />
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
